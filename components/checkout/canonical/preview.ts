import { subscriptionCheckoutPreview } from '@/components/checkout/subscription/canonical/preview';

// Editor-only fixture. It is never stored in the Puck seed or used by pages.
export const checkoutPreview = {
  cart: subscriptionCheckoutPreview.cart,
  initialEmail: 'avery@example.com',
};
