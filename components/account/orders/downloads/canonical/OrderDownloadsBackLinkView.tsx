import type { OrderDownloadsPageData } from '@/enigma-components/orders/downloads-canonical/orderDownloadsRuntime'; import { OrderDownloadsBackLink } from '@/enigma-components/orders/downloads-canonical/OrderDownloadsPageSections'; import { loadOrderDownloadsRuntime } from './orderDownloadsRuntime'; import { resolveOrderDownloadsData } from './viewData';
interface Props { data?: OrderDownloadsPageData | null; puck?: { isEditing?: boolean }; }
export const puckComponentName = 'OrderDownloadsBackLink'; export const puckLabel = 'Back To Order Details'; export const puckCategory = 'Account'; export const puckFields = {}; export const puckDefaults = {}; export const puckAst = { kind: 'runtime', sourceJsxNames: ['OrderDownloadsBackLink'], sourceImportPaths: ['@/components/orders/canonical/OrderDownloadsPageSections'], role: 'order-downloads-back-link', slotTarget: 'back', runtimeSignals: ['order.id'] };
export async function puckDataFetcher(_props: Props, context?: Parameters<typeof loadOrderDownloadsRuntime>[0]) { return loadOrderDownloadsRuntime(context); } export function OrderDownloadsBackLinkView(props: Props) { const data = resolveOrderDownloadsData(props); return data ? <OrderDownloadsBackLink data={data} /> : null; }
